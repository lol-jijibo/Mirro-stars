# Mirro Backend — AI问题解决引擎依赖
# 核心Web框架：FastAPI + uvicorn
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# 数据库：MySQL异步驱动
aiomysql>=0.2.0

# LLM调用：OpenAI SDK（兼容 OpenAI / Claude / 通义千问等）
openai>=1.50.0

# 搜索聚合：Tavily Search API
tavily-python>=0.5.0

# 环境变量管理
python-dotenv>=1.0.0

# SSE流式输出支持
sse-starlette>=2.0.0

# HTTP客户端（搜索服务用）
httpx>=0.27.0
